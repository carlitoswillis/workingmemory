# Copy to .env.local. For local dev these are the standard values `supabase start`
# prints; for a hosted project use the URL + anon key from your Supabase dashboard.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
