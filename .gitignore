/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo

# local working-memory database (rebuildable cache of your own data — keep it off git)
/data

# downloaded backups of the hosted DB (your personal data — never commit)
/backups

.DS_Store
*.pem
.env*.local
npm-debug.log*
.aider*

# local AI-assistant session state (machine-specific, not project docs)
/.claude/
